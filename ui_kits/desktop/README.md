# Desktop UI kit — Keen

Interactive click-through of core desktop app screens: Home dashboard
(sidebar + game catalog), Game Detail, Progress. Built at a 1280px desktop
viewport. Composes the shared component library in `../../components/`.
